{
  "name": "remembering-professor-tony",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:preview": "wrangler versions upload",
    "test": "node --test tests/memorial.test.mjs tests/remotion-video.test.mjs",
    "video:dev": "npm --prefix video run dev",
    "video:capture": "npm --prefix video run capture",
    "video:check": "npm --prefix video run check",
    "video:render": "npm --prefix video run render"
  },
  "devDependencies": {
    "wrangler": "4.61.1"
  }
}
